
<nav class="navbar navbar-default">
    <div class="container">

        <div class="navbar-header">

            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="fa fa-bars"></span>
            </button>
            <a class="navbar-brand" href="{{ path('kreatys_cms_home') }}">
                {% if params.logo %}
                    {% media params.logo, 'petite' with {'class': 'img-responsive'} %}
                {% else %}
                    Kreatys CMS
                {% endif %}
            </a>
        </div>

        <div class="collapse navbar-collapse" id="navbar-collapse-1">
            {{ knp_menu_render('main', {'ancestorClass': 'active', 'currentAsLink': true, 'currentClass': 'active', 'template': 'KreatysCmsBundle:Front/Menu:knp_menu.html.twig'}) }}
        </div>
    </div>
</nav>